# 2026-09-15 interrupted adoption recovery

用户要求优先恢复现有 runtime 权威状态，禁止重复 adopt，禁止测试/验证，不 commit/push。

读取的已有事实：adopt-20260915.status=1；adopt log 已停止 Local Dev/CodeGraph，随后 Broker 等待失败；Broker log 却已写出 ready，PID 14199 仍是 workspace tools/browser/mcp-shared-broker/dist/server.js。endpoint=127.0.0.1:49250/mcp，authority file=v2 OPEN/generation 0；Local Dev runtime 元数据 stopped，旧 target 仍为 Desktop Commander direct stdio。

执行：复用上述 Broker，依次为 local-dev/codegraph/repomix/playwright-chrome 调用一次 runtimes connect，四次均返回 Connected。Local Dev target 改为 Broker HTTP；Playwright 使用 workspace shared broker wrapper。没有调用 adopt、stop-all、authority prepare/claim/promote/revoke，没有创建额外 raw Playwright controller。

修正：loopback curl bypass proxy；Broker PID 存在但 endpoint 不可用时不启动第二实例；新增局部 reconcile/doctor 入口；外部 Browser wrapper 改为 workspace 转发并保留原 installed backup；launchd 模板归 workspace，投影写回但未 reload/bootstrap。

这不是验收：没有 tools/list、工具调用测试、read/write fencing 实验、Browser approve、Microsoft auth/create/host、build/typecheck/lint。Connected 不证明 tools usable 或 per-Chat identity。当前 authority OPEN 也不代表 Monitor old/new consumer fencing 已完成。后续要验收须单独开放。

Git：保留原 workspace 已 staged/unstaged WIP 与四个产品 WIP；本轮没有 stage/commit/push/reset/clean。Dev Tunnel source 与产品 IPC 边界改动未经过类型或行为验证；ProFlow 原有 native-command mocks 需要后续按 workspace resource protocol 调整。

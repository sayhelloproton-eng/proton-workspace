# 92｜实验与数据索引

这个索引只记录会改变设计判断的真实实验，不收集没有结论价值的命令输出。

| ID | 阶段 | 对比/问题 | 指标 | 结论 | 详情 |
|---|---|---|---|---|---|
| EXP-001 | P1-E | EmbeddingGemma Q8 vs dense Q4_K | Q4↔Q8 cosine mean=0.9894；pairwise correlation=0.998129；8/8 Top1 | dense Q4_K 维持语义空间质量并显著降低 CPU 成本 | `02_P1_Knowledge_Management知识构建.md` |
| EXP-002 | P1-E | 全 Corpus token profile | 7,624 chunks；99.51% <=2048 tokens；37 超长 | 超长 Chunk 在 Derived Index 派生 windows，不回改 Source Chunk | P1-E |
| EXP-003 | P1-F | FTS OR vs exact indexed substring vs full-query fuzzy trigram | Recall@10=0.30 / 1.00 / 0.60；MRR=0.15 / 0.8533 / 0.3843 | exact substring 适合工程术语；full-query trigram 拒绝 | P1-F2 |
| EXP-004 | P1-F | full-corpus exact pgvector | 7,670 vectors；p50≈300~341ms；p95≈505~552ms | 先保留 exact control group，不提前加 ANN | P1-F4 |
| EXP-005 | P2-B | snapshot-bound read path | lexical=10；vector=10；self-query Top1 distance=0；hydration=6/6 | KnowledgeReadContract / pgvector / DTO hydration 链路成立 | `03_P2_Retrieval查询检索.md` |
| EXP-006 | P2-C | Hybrid candidate merge + 在线 Query Embedding | fixture 10+10→19 unique/overlap1；真实 text query union=20；runtime model guard PASS | 两路召回可真实组合且不需要伪造 fused score；模型身份必须查 `/v1/models` | `03_P2_Retrieval查询检索.md` |
| EXP-007 | P2-D | RRF 是否真正只依赖 branch rank | 修改 lexical score/vector distance 极值但保持 rank，fusion ordering/score 完全不变；真实 query fused candidates=20 | RRF 可安全融合异量纲检索器，`k` 仍留给 P2-F Eval | `03_P2_Retrieval查询检索.md` |

| EXP-008 | P2-E | reranker runtime / model candidate A/B | Qwen Q8 3 docs≈57.53s但排序正确；Qwen Q4≈11.65s但排序错误；BGE Q4 sanity≈5.51s；真实 `3×200`≈6.15s（重复约6~11s） | 选择 BGE Q4_K_M 作为 Mechanical candidate；candidate/text budget 与 latency baseline 留 P2-F | `03_P2_Retrieval查询检索.md` |

| EXP-009 | P2-F | 13-case lexical/vector/Hybrid/RRF sweep | lexical R@10=0.6923；vector=0.8462；Hybrid@20=1.0；d20/k30 RRF R@10=1.0/MRR=0.6581 | 按 Recall-first 冻结 depth20/k30；d5 虽 MRR 更高但漏 Gold | `03_P2_Retrieval查询检索.md` |
| EXP-010 | P2-F | 最终 d20/k30 + BGE top3×200 持续 operational A/B | 15s eval ceiling：0/13 reranked，13/13 timeout fallback；fallback R@10/MRR 完全保持 | Reranker capability 保留，但当前 Intel baseline 默认 OFF | `03_P2_Retrieval查询检索.md` |

## 后续优先实验

- lexical-only vs vector-only vs hybrid retrieval。
- Hybrid candidate depth 与 duplicate rate。
- RRF 参数对 Recall/MRR/ordering 的影响。
- RRF-only vs RRF + Reranker。
- exact pgvector vs ANN 的 latency + Recall A/B（只有 Latency Budget 证明需要时）。
- Evidence/Context budget 对最终 RAG consumer 可用性的影响。

Generation FAST/THINK、Conversation 和 Chat UI 实验已经移交独立 ChatWeb 工程，不再属于本 RAG 实战索引。

## P8 Runtime / Throughput｜2026-09-08

- Runtime targeted：API lifecycle / public health / embedding degradation / DB unavailable / system status 全 PASS。
- Formal Gate attempt2：exit 0；P6 13-case capability regression 与 retrieval degradation 全 PASS。
- P8 throughput sample：initial blast radius=23 files，final semantic implementation=24，DCR≈95.8%；implementation mutation calls=3，Mutation Density≈8.0 files/call，batch retry=0。
- 已知 verification windows：4m27s + 8m35s(failed harness gate) + 2m02s + 7m02s；不据此伪造完整 EER。


## V0 Release｜2026-09-08

- 正式 `knowledge:rebuild` 对当前 ACTIVE commit 真实执行：806 documents / 7624 chunks / 7670 embeddings / pending=0；复用 COMPLETE index，默认 activation unchanged。
- `verify:v0` durable Gate 完整 exit 0，约 8m28s；Knowledge consistency / Retrieval / Citation / 13-case RAG Eval / degradation / Runtime / ChatWeb 22/22 consumer / real E2E 全 PASS。
- V0 public RAG Eval mechanical latency：p50=1347.88ms，p95/max=1712.56ms；paired ChatWeb consumer overhead=151.24ms。
- 一次机器重启导致 Gate evidence 不可恢复，严格记为 UNKNOWN；恢复既有 Postgres.app PG17 cluster 后改用 `.runtime/release` durable evidence 完成最终 Gate。

## Post-V0 Consolidation｜2026-09-08

- live repository audit：`apps/` 最终只有 `api`；真实 PostgreSQL schema 最终只有 `knowledge + system`；空 `answering/quality` 通过 0005/0006 无 CASCADE migration 删除。
- CURRENT 从约 496 行阶段历史 dump 收敛到约 134 行 Maintenance rolling truth。
- consolidation tracked diff 观测约 67 files / 411 insertions / 1239 deletions；主要是 archive/delete/authority rewrite，不代表 67 次 mutation。
- `verify:consolidation` durable Gate exit 0：ChatWeb consumer 22/22 PASS，real E2E PASS，DB read-only PASS；paired consumer overhead 本轮约 257ms，仍只作为机械观测，不是 SLO。

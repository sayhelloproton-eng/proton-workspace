# 90｜问题与排障索引

这个文件不替代阶段正文，只负责把值得复盘/面试的真实问题串起来。

| ID | 阶段 | 现象 | 根因 | 结果 | 详情 |
|---|---|---|---|---|---|
| ISSUE-000 | 设计期 | Spec 一度按技术组件平铺 | 把技术流水线误当完整 SDD/DDD ownership | 重构为 SDD + DDD Context + Capability + Verification | `00_项目起点与架构基线.md` |
| ISSUE-P0-001 | P0 | TypeScript 7 `moduleResolution=node10` 失效 | 工具链已淘汰旧 Node resolution | 切到 NodeNext/ESM，Gate PASS | `01_P0_工程与SDD执行骨架.md` |
| ISSUE-P1-001 | P1-E | 手机模型可聊天但 `/v1/embeddings` 404 | Runtime capability contract 缺失，不是模型“不会 embedding” | Embedding 回退 Mac llama.cpp | `02_P1_Knowledge_Management知识构建.md` |
| ISSUE-P1-002 | P1-E | 第一份 Q4 与 Q8 同文本 cosine 接近 0 | 社区 GGUF 缺 dense modules，不是公平量化候选 | 换 dense Q4_K 并做 space parity A/B | P1-E |
| ISSUE-P1-003 | P1-F | 全量 supervisor 中断且首版缺最终 exception 证据 | 后台监督脚本可观测性不足，后续又暴露 PID 路径假设错误 | v2 加 child exit/stdout/stderr/FATAL/live PID，并分 token bucket 恢复 | P1-F4 |
| ISSUE-P1-004 | P1-F | token_count<=512 的 3 行实际 tokenizer 得到 513 | 预计算 bucket 与 runtime tokenizer 边界有 1 token 偏差 | 临时 ubatch1024 处理 edge rows 后恢复默认 | P1-F4 |
| ISSUE-P1-005 | P1-H | 阶段 Eval RED：缺完整 build failure audit | 只有 index failure，没有 IngestionRun lifecycle | 新增 0004 + IngestionRun persistence | P1-H |
| ISSUE-P1-006 | P1-H | 重算 chunk-set hash 假失败 | PostgreSQL text ordering 与 JS `localeCompare` canonical ordering 不同 | Eval 复用原 canonical ordering，不修改数据 | P1-H |
| ISSUE-P2-001 | P2-B | Published Contract 初版仍 import Knowledge Domain 类型 | 运行边界干净，但类型层仍泄漏内部模型 | 改为 contract-owned DTO | `03_P2_Retrieval查询检索.md` |
| ISSUE-P2-002 | P2-C | 错误 model id 仍通过 `/v1/embeddings` response model guard | llama.cpp 回显请求中的 model，不能证明实际 loaded model | 改用 `/v1/models` 读取运行时权威模型身份 | `03_P2_Retrieval查询检索.md` |

| ISSUE-P2-003 | P2-E | iPhone 有模型但常见 rerank 路由全部 404；随后 Q8 太慢、Q4 sanity 排错 | “设备有模型/端点能跑”被误当成 reranker contract 与可用性 | 改为 capability probe + 同一 sanity/latency A/B，最终冻结 BGE Q4_K_M；失败统一回退 RRF | `03_P2_Retrieval查询检索.md` |

| ISSUE-P2-004 | P2-F | 第一轮 online lexical Recall@10 仅 0.1538，与 P1 EXACT_INDEXED=1.0 明显冲突 | P2-B/C 只接 FTS_OR，遗漏 P1 已验证 exact signal | 补 deterministic engineering exact term/phrase + indexed substring；最终 lexical R@10=0.6923，Exact 子集=1.0 | `03_P2_Retrieval查询检索.md` |
| ISSUE-P2-005 | P2-F | full Eval 初版强制 13/13 必须 RERANKED，第 8 题合法 timeout fallback 被误判失败 | Eval harness 把已设计 degradation 当机械错误 | 改成 degradation-aware operational A/B，记录 reranked/fallback/timeout rate；最终 13/13 timeout 支持默认 OFF | `03_P2_Retrieval查询检索.md` |

| ISSUE-V0-001 | V0 | `pnpm knowledge:rebuild run` 首次把前导 `--` 当成 command | CLI parser 没兼容 pnpm 参数分隔符 | 在 DB mutation 前 fail；只修 parser 后正式 operator 跑通 | `10_V0_RAG_Capability_Release.md` |
| ISSUE-CONS-001 | Post-V0 | V0 已完成但 live tree 仍混有 Site/Conversation/Generation/Phone/Tunnel 等 superseded 资产 | “标记 superseded”没有等于“退出当前设计真源” | legacy spec 迁历史区，placeholder 删除，Architecture Gate 禁止回流 | `10_V0_RAG_Capability_Release.md` |
| ISSUE-CONS-002 | Post-V0 | P2A Gate 在当前架构下假失败 | harness 永久硬编码阶段状态 `FROZEN_P2A_2026-09-07` | 改为验证当前 Query ownership + legacy archive，不再把历史状态当永久事实 | `10_V0_RAG_Capability_Release.md` |

## 记录规则

只记录真实发生的问题。每条至少保留触发现象、错误假设、根因、验证证据与修复影响；普通工具噪声不升级成项目问题。

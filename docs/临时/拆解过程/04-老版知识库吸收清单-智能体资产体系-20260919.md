# 老版知识库吸收清单｜智能体资产体系｜2026-09-19

## 正式 Owner

`docs/知识库/专业Agent资产化.md`

正式文章已从旧 06 六篇与当前 Agent / Runtime / Harness / Eval / Behavior Release / ProFlow 实践重新建模，并在开头补齐当前真实方案：长期 Role → Task / Binding → Worker / Conversation → Runtime Reality → Harness → Node → Artifact / State / Evidence → Handoff → 独立验证与恢复。

## 已吸收的稳定判断

- Role 不等于 Model、Host、Worker、Conversation；
- Profile 只组合稳定资产，不复制其他 Owner 真值；
- 当前 Task、State、Approval、Evidence、Secret 属于运行时事实；
- Context 每轮重新装配，Memory 不能覆盖当前权威现实；
- 信任来自 Eval 与真实 Evidence；
- 发布对象是完整 Behavior Release；
- 失败先进入 Evidence，再修真正 Owner，并留下 Regression；
- 第二个真实消费者才开始证明公共资产化；
- Agent 可以合并、降级和退役。

## 已退休的历史设计

固定 Role/Profile YAML、预设 `agents/` / `knowledge-packs/`、Catalog、Release Registry、Publisher、P0-0～P0-9 和旧候选 Agent 清单不再作为当前推荐架构。

## 清理结果

2026-09-19 用户确认当前正式文章可接受并要求清理：

- 旧 06 六篇正文已替换为兼容迁移桩；
- `VIS-035` SVG / PNG 已删除；
- 旧路径仍可打开并指向当前 Owner，避免保留的旧 04 / 05 / 07 / 10 文档断链；
- 没有 commit / push。

当前状态：**06 智能体资产体系正文已退休。**
